export const ITEM_LIST = (boardID: number) => {
  return `itemList_${boardID}`;
};

export const HORIZONTAL = (boardID: number) => {
  return `horizontal_${boardID}`;
};

export const VERTICAL = (boardID: number) => {
  return `vertical_${boardID}`;
};

export const PREFERENCE = (boardID: number) => {
  return `preference_${boardID}`;
};
